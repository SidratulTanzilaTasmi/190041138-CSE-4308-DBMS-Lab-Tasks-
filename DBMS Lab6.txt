1.
SELECT MAX(SALARY)
FROM CITIZEN;
2.

SELECT C_NAME FROM CITIZEN 
WHERE GENDER = 'Male' AND 
SALARY =(SELECT MAX(SALARY) 
FROM 
(SELECT * FROM CITIZEN WHERE GENDER = 'Male')
);

3.

SELECT C_NAME FROM CITIZEN WHERE GENDER = 'Female' AND 
SALARY =(SELECT MIN(SALARY) 
FROM 
(SELECT * FROM CITIZEN WHERE GENDER = 'Female')
);

4.

SELECT C_NAME FROM CITIZEN WHERE OCCUPATION = 'Teacher' AND 
SALARY =(SELECT MAX(SALARY) 
FROM 
(SELECT * FROM CITIZEN WHERE OCCUPATION = 'Teacher')
);

5.

SELECT C_NAME FROM CITIZEN WHERE OCCUPATION = 'Doctor' AND 
SALARY =(SELECT MAX(SALARY) 
FROM 
(SELECT * FROM CITIZEN WHERE OCCUPATION = 'Doctor')
);

6.

SELECT C_NAME FROM CITIZEN WHERE C_NAME LIKE '%a%';

7.

SELECT C_NAME FROM CITIZEN WHERE C_NAME LIKE '__y%';

8.

SELECT AVG(AGE) FROM CITIZEN;

9.

SELECT SUM(SALARY) FROM 
(SELECT SALARY FROM 
CITIZEN WHERE OCCUPATION = 'Doctor');

10.


 SELECT AVG(SALARY) 
 FROM 
 (SELECT SALARY FROM CITIZEN WHERE OCCUPATION = 'Student');

11.

SELECT C_NAME FROM CITIZEN WHERE OCCUPATION = 'Student' AND GENDER = 'Female' 
AND 
SALARY=(
SELECT MAX(SALARY) 
FROM 
(SELECT SALARY FROM CITIZEN WHERE OCCUPATION = 'Student')
);

12.

SELECT COUNT(C_HOME) FROM 
(SELECT C_HOME FROM CITIZEN GROUP BY C_HOME) 
WHERE C_HOME LIKE '%tg%';

13.

SELECT MAX(SALARY) FROM CITIZEN WHERE C_NAME LIKE 'A%';

14.

SELECT * FROM CITIZEN WHERE
SALARY=
(SELECT MIN(SALARY) 
FROM 
(
SELECT SALARY FROM CITIZEN WHERE OCCUPATION = 'Student'
)
);

15.

SELECT AVG(AGE) FROM 
(SELECT AGE FROM CITIZEN WHERE OCCUPATION = 'Student');

16.

SELECT COUNT(C_NAME),C_HOME FROM CITIZEN GROUP BY C_HOME;

17.

SELECT MIN(SALARY) FROM CITIZEN GROUP BY C_HOME;

18.

SELECT AVG(SALARY) FROM CITIZEN WHERE C_HOME= 'Dhaka' GROUP BY OCCUPATION;

19.

SELECT AVG(SALARY) FROM CITIZEN WHERE GENDER = 'Female' 
AND SALARY >= 10000 GROUP BY OCCUPATION;

20.

SELECT COUNT(C_NAME) FROM CITIZEN WHERE SALARY NOT BETWEEN 40000 AND 50000;

21.

SELECT COUNT(C_NAME) FROM CITIZEN WHERE SALARY >= 20000 AND C_HOME = 'Ctg';

22.

SELECT AVG(SALARY) 
FROM CITIZEN WHERE SALARY >=55000 
GROUP BY OCCUPATION
HAVING COUNT(C_ID)>1;

23.

SELECT dis.name AS Name_of_district , dis.size AS size_of_district , COUNT(cit.id) AS total_number_of_citizen 
FROM Districts AS dis , 
Citizens AS cit 
GROUP BY cit.district;

24.

SELECT dis.name AS Name_of_district FROM
(SELECT cit.district, 
COUNT(cit.id) FROM Citizens AS cit 
ORDER BY cit.district
) 
as dis;

25.

SELECT dis.name As Name_of_district 
FROM
(
SELECT COUNT(c.citizen)
FROM
(
SELECT cit.id
FROM cit
GROUP BY cit.district
CITIZENS AS cit
)
INJECTED_CITIZEN AS c
)
row_number() over (order by dis.name) AS least_dis
WHERE least_dis<=3;
District as dis;